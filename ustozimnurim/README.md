# UstozimNurim — React + Vite + Tailwind Starter

Quick start:

1. Install dependencies

```bash
npm install
```

2. Start dev server

```bash
npm run dev
```

3. Build CSS (if needed)

```bash
npm run build:css
```

Notes: Tailwind is configured via `tailwind.config.cjs`. `src/index.css` includes Tailwind directives.
